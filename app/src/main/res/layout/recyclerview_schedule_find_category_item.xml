<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:id="@+id/item_category_list"
    android:gravity="center"
    android:layout_marginLeft="5dp">

    <ImageView
        android:id="@+id/recyclerview_category_color"
        android:layout_width="42dp"
        android:layout_height="27dp"
        android:src="@drawable/category_basic_squre"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/recyclerview_category_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:fontFamily="@font/noto_sans_cjkkr_medium"
        android:gravity="center"
        android:text="학교"
        android:textSize="12sp"
        android:textStyle="normal"
        app:layout_constraintBottom_toBottomOf="@+id/recyclerview_category_color"
        app:layout_constraintEnd_toEndOf="@+id/recyclerview_category_color"
        app:layout_constraintStart_toStartOf="@+id/recyclerview_category_color"
        app:layout_constraintTop_toTopOf="@id/recyclerview_category_color"/>

</androidx.constraintlayout.widget.ConstraintLayout>